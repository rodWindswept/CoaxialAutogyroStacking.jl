# Ticket 04: Build shared vocabulary glossary

Type: `wayfinder:grilling` (HITL)

## Question

What terms in this codebase have specific, non-obvious meanings that
an agent must know to work correctly? Extract a glossary.

Examples of candidate terms: "anchor" vs "ground station," "tension"
vs "force along line," "tilt" vs "elevation," "PCA" vs "PCA-2," "R1"
vs "topmost rotor."

Use `/domain-modeling` to extract the glossary. Output as a markdown
file linked from both AGENTS.md and CLAUDE.md.
