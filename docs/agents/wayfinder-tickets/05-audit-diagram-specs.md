# Ticket 05: Audit diagram SPECs for contradictions and drift

Type: `wayfinder:task` (AFK)

## Question

Do the SPEC.md files in `diagrams/*/` agree with each other and with
the central AGENTS.md writing rules? Find contradictions, stale
references, and formatting inconsistencies.

Check: every SPEC has all 12 mandatory sections from the
`coaxial-diagram-registry` skill. Check that data sources, variable
names, and units are consistent across related diagrams (e.g., the
bem-feasibility pair, the bem-tension pair).
