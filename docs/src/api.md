# API Reference

```@meta
CurrentModule = CoaxialAutogyroStacking
```

```@index
```

All exported symbols. Docstrings render here as they are added to the source —
keeping `@autodocs` means new exported functions appear automatically.

```@autodocs
Modules = [CoaxialAutogyroStacking]
Order   = [:type, :function]
```
